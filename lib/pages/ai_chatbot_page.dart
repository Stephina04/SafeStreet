import 'package:flutter/material.dart';

class AIChatbotPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("AI Chatbot")),
      body: Center(child: Text("AI Safety Assistant Page")),
    );
  }
}
